import 'package:flutter/material.dart';

class ScoutingPages extends StatelessWidget {
  const ScoutingPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
